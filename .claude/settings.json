{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in *.py) cd \"$CLAUDE_PROJECT_DIR\" || exit 1; ruff format \"$f\"; out=$(ruff check \"$f\" 2>&1) || { echo \"$out\" >&2; exit 2; } ;; esac"
          }
        ]
      }
    ]
  }
}
